apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: payment-service-stage
spec:
  selector:
    app: payment-service
  ports:
    - protocol: TCP
      port: 8400         # Ajusta el puerto si es necesario
      targetPort: 8400
  type: ClusterIP
